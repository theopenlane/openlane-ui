query GetVendor($where: EntityWhereInput) {
  entities(where: $where) {
    edges {
      node {
        createdAt
        createdBy
        description
        displayName
        domains
        status
        tags
        updatedAt
        updatedBy
      }
    }
  }
}
