import { GENERATED_MODULES_BY_OBJECT_TYPE } from '@repo/codegen/src/object-modules'

// the modules an organization needs for each object type, only one of them is required
// object types without an entry are not gated
export const MODULES_BY_OBJECT_TYPE = GENERATED_MODULES_BY_OBJECT_TYPE
